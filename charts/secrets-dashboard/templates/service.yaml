apiVersion: v1
kind: Service
metadata:
  name: {{ include "secrets-dashboard.fullname" . }}
  labels:
    {{- include "secrets-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secrets-dashboard.selectorLabels" . | nindent 4 }}
